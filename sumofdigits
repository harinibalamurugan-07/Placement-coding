#include<stdio.h>
int main(){
    int n,sum=0;
    printf("Enter the number:");
    scanf("%d",&n);
    int q=1,r;
    while(q!=0){
        q=n/10;
        r=n%10;
        sum=sum+r;
        n=q;
        
    }
    printf("%d",sum);
    
}
