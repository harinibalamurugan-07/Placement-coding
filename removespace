#include<stdio.h>
#include<string.h>
int main(){
    char s[100];
    int l=0;
    printf("Enter the string:");
    fgets(s,sizeof(s),stdin);
    while(s[l]!='\0'){
        l++;
    }
    for(int i=0;i<=l-1;i++){
        if(s[i]!=' ')
        printf("%c",s[i]);
    }
}
